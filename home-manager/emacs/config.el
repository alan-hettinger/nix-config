;;; config.el --- Locally-defined settings -*- lexical-binding: t-*-

;;; Commentary:
;; This file sets preferences for locally-defined settings.
;; It shouldn't contain any complex logic.

;;; Code:

(custom-set-variables
 '(alan/preferred-file-tree 'treemacs)
 '(alan/dired-style 'simple)
 )

(provide 'config)
;;; config.el ends here.
